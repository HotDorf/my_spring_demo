动态代理分为基于子类的动态代理和基于接口的动态代理
    被代理的对象经过的方法都会经过动态代理
    jdk代理是，初始化代理的时候让其与被代理对象有 相同的类加载器、相同的接口，代理对象与被代理对象是“兄弟”关系。写InvocationHandler的匿名内部类，使用反射来增强方法或是字段。
    cglib代理是，初始化代理时让其获取被代理对象的 类字节码，代理（子）与被代理（父）对象是父子类关系，写MethodInterceptor的匿名内部类，内部类不同的是cglib比jdk多一个方法代理的参数。增强的方式也是反射。

    spring aop是基于动态代理实现的，通过强化方法来解耦合。比如通过事务对象的动态代理，将对数据库的操作和事务进行分离。

测试在SpringAOPTest类
